<%= blob.filename %>
<%= image_tag blob.representation(resize_to_limit: [100, 100]) %>
<%= number_to_human_size(blob.byte_size) %>
<%= link_to 'Delete', blob, method: :delete, data: { confirm: 'Are you sure?' } %>
